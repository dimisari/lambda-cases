
Example:

or_type A, B :-> TypeName
values
  Case0 A | Case1 (b_value: B, int_value: Int) | Case2 ( A, B ) | Case3 ListOf(A)s

Comments:

  - default value_name for the input of all Cases: case_value

    Examples:

    fun1: (A, B :-> TypeName) -> A-->ValueOrNothing
      = cases
        Case0 -> Value case_value
        ... -> Nothing

    fun2: (A, B :-> TypeName) -> (A, B)-->ValueOrNothing
      = cases
        Case2 -> Value case_value
        ... -> Nothing

  - anonymous tuple_types can be used for naming fields of a Case

    Example function:

    fun: (A, B :-> TypeName) -> B-->ValueOrNothing
      = cases
        Case1 -> Value b_value
        ... -> Nothing

  - tuple_types can be used in a Case and there is field inheritance

    Example function:

    fun: (A, B :-> TypeName) -> A-->ValueOrNothing
      = cases
        Case2 -> Value pair_first
        ... -> Nothing

  - is functions should be generated automatically:

    isCase0: (A, B :-> TypeName) -> Bool
    isCase1: (A, B :-> TypeName) -> Bool
    ...
