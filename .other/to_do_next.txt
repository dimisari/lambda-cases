
* allow big function bodies to indent deeper to have a scope for a "where"

* fix grammar to match the new parser
   * TUPLE TYPE, OR TYPE, TUPLE TYPE

* lists and tuple in value def propositions
  * instead of "all <Type>"?
  * e.g.

    [x,y,z] : ListOf(Int)s
      = [1,2,3]

    (x,y) : Int x String
      = (1, "hi")

* finish comments and descriptions
  * Generation
    * add README.md
  * Preprocessing
    * add README.md

* test/expected_outs
  * run all compiled programs with some automated interaction and check against
    correct results

* test/outputs/trees
  * ASTs using dot
    * I can literally graph the whole AST and all subtrees

# to_do.txt
