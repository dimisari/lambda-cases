
- αλλαγή syntax:

  - tuple type: State = ListOf(DoneNode)s x ListOf(ReachableNode)s
    field names: (dns, rns)

  - or type: Possibly(T1)
    possibilities: a_value.with:(the_value: T1) | no_value

  - type nickname: ...

  - ? αντί για cases
    - Αλλαγή ονομάτων στο ASTTypes.hs

# to_do.txt

