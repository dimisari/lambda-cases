
- Έχω ξεχωριστά hardcoded τα:
  - test/output/
  - test/input/
  στο src/grules.hs και στο Makefile
  - Να τα περάσω σαν arguments από το Makefile στο src/grules.hs;
    - Δεν είναι κακό

- fix README

- αλλαγή syntax:
  - >> αντί για ;>
  - ? αντί για cases
  - tuple type: State = ListOf(DoneNode)s x ListOf(ReachableNode)s
    field names: (dns, rns)
  - or type: Possibly(T1)
    possibilities: a_value.with:(the_value: T1) | no_value
  - type nickname: ...

- printing tuples:
  - (a = 1, b = 2)

- group type nicknames with "all"

- comments (do we really need them?)
  - line: #
    - can also close in the same line with second #
  - multiline: ## and close with second ##

- add element index mechanism
  - .<int-lit> for element
  - .change for changing
    - (i, j) = ...

- arrays
  - how?

- autogenerated functions:
  - is_case: OrType -> Bool
    put in map + generate definition
  - make it clearer

- add imports

- rewrite compiler in lcases?

- translate some Haskell libraries?

# to_do_next.txt
