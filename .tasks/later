- indexes in predefined list operations should start with 1
  - Make a list them and check if they all adhere
- test/outputs
  - types (+ propositions and theorems) only
  - values only
- group error messages in seperate file
- Να φτιάξω 2 διαφορετικά ASTs με διφορετικά types (πριν και μετά το preprocessing)
  - Αλλαγές
    - TypeTheo να έχει μόνο ένα PropNameWithSubs στο 1ο και όχι λίστα
      - Η λίστα χρειάζεται για το 2o
    - Identifier
      - Κάπως να βρω ένα καλύτερο representation με το UndersInParen
- Να γράφω τα errors των grammar rules σε άλλο αρχείο
- Allow identifiers to start with underscore
  - id-start ::= '_' lower-or-under+ | lower lower-or-under*
- μετονομασία cases func expr σε qmark func expr
- default printing tuples:
  - (a = 1, b = 2)
- comments (do we really need them?)
  - line: #
    - can also close in the same line with second #
  - multiline: ## and close with second ##
- add element index mechanism
  - .<int-lit> for element
  - .change for changing
    - (i, j) = ...
- add imports
- reading files into bytestring instead of string?
  - src/lcc
- Change syntax of type proposition and theorem
- test/expected_outs
  - run all compiled programs with some automated interaction and check against correct results
- test/outputs/trees
  - ASTs using dot
    - I can literally graph the whole AST and all subtrees
